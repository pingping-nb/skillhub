# 外部資料庫部署
# 使用方式：kubectl apply -k overlays/external/
# 前置條件：修改 base/configmap.yaml 和 secret.yaml 中的資料庫連線地址

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: skillhub

resources:
  - ../../base

labels:
  - pairs:
      app.kubernetes.io/deployment: external-db
