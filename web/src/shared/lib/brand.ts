/**
 * Centralized brand identity for the Neobards SkillHub fork.
 *
 * This is a non-invasive extension: it isolates the fork's product-name
 * override (upstream: "SkillHub") in a single constant so the rename is a
 * one-line change rather than scattered hardcoded strings across pages.
 *
 * To revert to the upstream name, change this value back to "SkillHub".
 */
export const BRAND_NAME = 'Neobards SkillHub'
